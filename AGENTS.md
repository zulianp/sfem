# Task Master AI - Claude Code Integration Guide

## Local SFEM Environment

- Always use `/Users/patrickzulian/Desktop/code/merge_git_repos/sfem/venv` for Python commands in this repository.
- Prefer `venv/bin/python` over `python`, `python3`, or other system interpreters.
- When running tests or generators, set `PYTHONPATH=python` unless the command already handles repository bootstrapping.
