#ifndef SFEM_MACROS_H
#define SFEM_MACROS_H

#define MIN(a, b) ((a) <= (b) ? (a) : (b))
#define MAX(a, b) ((a) >= (b) ? (a) : (b))
#define POW2(a) ((a) * (a))
#define POW3(a) ((a) * (a) * (a))

#endif  // SFEM_MACROS_H
