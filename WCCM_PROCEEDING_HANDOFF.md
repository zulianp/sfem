# WCCM post-conference proceeding：论文写作交接总结

本文档用于交接给论文文件夹中的 Codex。依据目前的讨论、已有实验检查和日志截图整理；未完成的实验和未定位的问题必须与已验证结果区分。

## 1. 论文定位与范围

这是一篇以 preliminary numerical results 为主的 WCCM post-conference proceeding，用户给出的截止日期为 9 月 15 日。

主线：在有限应变超黏弹性有限元中，使用低精度存储 Prony 分支的历史变量，同时保留主要本构计算与求解过程的高精度，评估历史存储节省与完整耦合仿真的数值误差之间的权衡。

本篇暂时不纳入 replay。严格误差上界、长期稳定性和系统性 robustness 理论分析留到后续工作，不作为本篇的完成条件。

暂定标题：

> Numerical Assessment of Reduced-Precision Storage of Prony History Variables in Finite-Strain Viscoelasticity

用户原拟标题中的 “Prony Series terms” 容易被理解为降低 Prony 系数的精度；这里实际降低的是各分支历史变量的存储精度。

核心研究问题：

1. 不同存储策略能减少多少历史变量内存？
2. 历史变量参与完整耦合求解后，位移响应与非线性收敛受到多大影响？
3. 这些误差对网格分辨率是否敏感？

论文定位是 implementation and numerical assessment，不是普适稳定性证明，也不是已验证的 FSI 应用论文。

## 2. Introduction 的叙事

可以从阀门、隔膜泵等 FSI 应用中的黏弹性固体出发，说明复杂模型中内存需求的重要性，再引出 Prony 历史变量的存储成本。

建议顺序：

1. 应用动机：需要描述材料的时间依赖响应。
2. Prony 表示的代价：每个单元、每个积分点、每个分支都需要保存历史张量。
3. 网格与分支数增加时，历史存储按其乘积增长。
4. 本文研究能否降低历史存储精度，而保持主要计算精度。
5. 贡献：实现多种存储策略、明确核算内存、通过完整耦合算例评估误差和收敛表现。

注意：

- FSI、阀门和隔膜泵是应用动机，不要写成本文已经完成的验证算例。
- “此前没有人做过”需要文献检索支持，不能直接写成事实。
- 不要把历史存储节省写成整个求解器总内存的同等比例节省。

## 3. 方法：五种 per-QP 历史存储策略

主比较保持同一种 per-QP 历史表示，只改变数值存储格式：

| 策略 | 历史分量存储 | scale 的共享范围 |
|---|---|---|
| FP64 | FP64 | 无 |
| FP32 | FP32 | 无 |
| FP16 | FP16 | 无 |
| FP16 + tensor scale | FP16 | 每个单元、每个 QP、每个 Prony 分支的一个历史张量 |
| FP16 + element-Prony scale | FP16 | 每个单元、每个 Prony 分支，跨该单元所有 QP |

当前历史张量使用六个独立分量；scale 使用 FP32。

必须区分：

- **element-Prony scale**：仍保留每个 QP 的历史，只让多个 QP 共用一个 scale。
- **per_elem history**：每个单元只保留一组历史，改变了历史变量的空间表示。

这两者不是同一个策略。

低精度历史的使用过程可以写成：

1. 读取并恢复上一时间步的历史变量。
2. 使用恢复的历史参与当前内力与非线性求解。
3. 在时间步收敛后，根据收敛位移更新历史。
4. 将更新后的历史转换为所选低精度格式保存。

示意公式：

\[
Z_{n+1}=\alpha\widehat H_n+\beta\Delta S_{n+1}.
\]

普通存储为 \(\widehat H_{n+1}=Q_p(Z_{n+1})\)。带 scale 时，保存 \(h_{n+1}=Q_{16}(Z_{n+1}/s_{n+1})\)，读取时恢复为 \(\widehat H_{n+1}=s_{n+1}h_{n+1}\)。这些公式用于说明存储机制，不在本文展开严格误差上界。

当前 scale 是二的整数次幂。缩放可以改变可表示的数值范围，但不增加 FP16 的有效尾数位数，因此不能预设 scaled FP16 必然比普通 FP16 更准确。

写作时使用“主要计算与求解采用高精度”，避免未经核查地声称所有数据和所有运算均为 FP64，例如当前网格坐标使用 FP32。

## 4. 内存分析

记：

- \(N_e\)：单元数量；
- \(Q\)：每个单元的积分点数量，当前 HEX8 为 8；
- \(P\)：Prony 分支数量，当前为 4；
- 每个历史张量有 6 个独立分量；
- 当前实现维护 old/new 两套历史 buffer，scale 也有两套。

普通存储的持久 buffer 大小为：

\[
B_{\mathrm{plain}}=2N_eQP\cdot6b,
\]

其中 \(b=8,4,2\) 分别对应 FP64、FP32、FP16。

两种 scaled FP16 的大小为：

\[
B_{\mathrm{tensor}}=2N_eQP(6\cdot2+4),
\]

\[
B_{\mathrm{element\text{-}Prony}}=2N_eP(Q\cdot6\cdot2+4).
\]

当 \(Q=8\) 时：

| 策略 | 相对 FP64 的历史及 scale 存储 | 节省比例 |
|---|---:|---:|
| FP64 | 100% | 0% |
| FP32 | 50% | 50% |
| FP16 | 25% | 75% |
| FP16 + tensor scale | 33.33% | 66.67% |
| FP16 + element-Prony scale | 26.04% | 73.96% |

以一百万个单元、8 个 QP、4 个分支、old/new 双 buffer 为例：

| 策略 | 理论持久历史及 scale 存储，十进制 GB |
|---|---:|
| FP64 | 3.072 |
| FP32 | 1.536 |
| FP16 | 0.768 |
| FP16 + tensor scale | 1.024 |
| FP16 + element-Prony scale | 0.800 |

这是理论例子，不是实测峰值内存。以上不包含网格、位移、矩阵、求解器工作空间、其他状态变量和线程临时 buffer。论文应说明统计口径。

## 5. per_elem 的位置

如果保留 per_elem，将它放在方法与实验的最后，作为额外的空间存储压缩策略。

同精度、无 scale 时，per_elem 的历史 buffer 大小为：

\[
B_{\mathrm{per\_elem}}=2N_eP\cdot6b.
\]

相对 per-QP，它减少为 \(1/Q\)。但这不再是单纯的精度变化，会引入历史空间表示的差异。

建议先比较 per_elem FP64 与 per-QP FP64，再讨论是否叠加低精度。不要把两种影响混合后全部称作 FP16 量化误差。当前主线的五种 per-QP 实验不依赖这一扩展完成。

## 6. 完整耦合悬臂梁实验

五种策略分别运行完整求解，各自的历史变量都会影响其内力和后续位移；以相同网格上的 FP64 历史存储结果为参考。

当前配置记录：

| 项目 | 设置 |
|---|---|
| 几何 | 长 10、宽 1、高 1，沿 x 方向 |
| 单元 | HEX8 |
| 边界 | min-x 侧固定全部位移自由度；实际节点选取应在成稿前核对 |
| 载荷 | 沿 -y 的重力加速度 0.5 |
| 密度 | 1 |
| Mooney–Rivlin 参数 | C10=800.622，C01=800.108，K=4000 |
| Prony 权重 | [0.4, 0.4, 0.1, 0.05] |
| 松弛时间 | [1, 2, 5, 10] |
| 时间步长 | 0.005 |
| 请求终止时间 | 3 |
| Newmark 参数 | beta=0.25，gamma=0.5 |
| 非线性残差阈值 | 1e-8 |
| 接触、WLF | 关闭 |
| 输出间隔 | 每 5 步，即 0.025 |

材料参数定义、单位和离散公式应以源码及论文模型部分最终核对为准。

分辨率参数 \(r\) 对应：

\[
N_e=10r^3,\qquad N_{\mathrm{nodes}}=(10r+1)(r+1)^2.
\]

最终采用哪些分辨率尚待确定。讨论过 r=15/30/45，也试过其他分辨率，不能把计划列为已完成数据。

## 7. 从脚本到误差测量

主要入口：

- `scripts/run_coupled.sbatch`：申请计算资源、激活环境、启动比较脚本。
- `scripts/run_mr_visco_history_compare.sh`：生成共享网格，依次运行五个 case，再执行后处理。
- `scripts/run_mr_visco_case.sh`：设置单个 case 的历史模式、精度和 scale，调用测试程序。
- `scripts/compare_mr_visco_history.py`：读取输出位移，检查时间对齐与有限值，计算误差并画图。

每个分辨率内部使用同一网格和参数；候选解与该分辨率的 FP64 参考比较。不同分辨率的数组不能直接逐项相减。

### 7.1 全场相对位移误差

将全部节点的三个分量拼成向量：

\[
E_{\mathrm{rel}}(t)=100\frac{\|\mathbf u_{\mathrm{candidate}}(t)-\mathbf u_{64}(t)\|_2}{\|\mathbf u_{64}(t)\|_2}\quad(\%).
\]

这是离散节点向量的欧氏范数，不是经过体积或质量矩阵加权的连续场 L2 范数。初始参考位移为零时需单独说明处理方式；当前初始两者均为零。

### 7.2 最大绝对分量误差

\[
E_{\max}(t)=\max_{a,k}|u^{\mathrm{candidate}}_{a,k}(t)-u^{64}_{a,k}(t)|.
\]

它是所有节点、所有分量中的最大值，不是最大节点位移误差向量的模。

### 7.3 自由端响应

选择参考网格中 \(x=x_{\max}\) 的节点，计算其 y 位移的算术平均：

\[
\overline u_y(t)=\frac{1}{N_{\mathrm{tip}}}\sum_{a\in\mathrm{tip}}u_{y,a}(t).
\]

这是自由端节点平均，不是中轴线测量，也不是单个自由端中心点，更不是面积加权平均。

### 7.4 非线性收敛

检查每步是否满足残差阈值，统计迭代次数并确认没有 NaN/Inf。日志从 Iter 0 开始，统计 Newton 更新次数与残差计算次数时必须区分。

所有最大误差均指已导出时间点上的最大值。FP64 参考仍包含空间、时间离散误差和求解误差，不能称为解析真解。

## 8. 已检查的本地 r=15、T=3 结果

数据目录：

`build_release/coupled_local_r15_T3/`

其中：

- `cases/`：各 case 原始结果及日志。
- `compare/coupled_comparison.png`：合并结果图。
- `compare/coupled_comparison.csv`：合并误差数据。

已有检查记录：

- 33,750 个单元、38,656 个节点。
- 自由端 256 个节点。
- 五个 case 均完成，导出的位移数据有限，时间网格一致。
- 输出有 121 帧，覆盖 t=0 到 t=3，间隔 0.025。
- 当前浮点时间循环实际执行了 601 个求解步，最后推进至约 3.005；最后导出的比较帧仍为 3。成稿时不要把求解步数与输出帧数混淆。
- 日志中的所有已完成时间步最终残差均小于 1e-8。

全场误差汇总：

| 候选策略 | t=3 相对位移误差 (%) | 导出时刻最大相对误差 (%) | t=3 最大绝对分量误差 |
|---|---:|---:|---:|
| FP32 | 2.52e-8 | 2.52e-8 | 3.47e-10 |
| FP16 | 9.87e-4 | 1.00e-3 | 1.46e-5 |
| FP16 + tensor scale | 8.74e-4 | 9.43e-4 | 1.29e-5 |
| FP16 + element-Prony scale | 9.42e-4 | 1.02e-3 | 1.41e-5 |

特别注意：0.001% 对应约 1e-5 的误差比值，不是 0.1%，也不是 0.001 的误差比值。

FP64 自由端平均 y 位移在 t=3 时约为 -1.91708847。候选减参考的自由端平均位移差分别约为：

- FP32：+1.82e-10；
- FP16：-9.00e-6；
- tensor scale：-4.14e-6；
- element-Prony scale：-5.60e-6。

按最终 Iter 编号统计的 Newton 更新次数总计：

| 策略 | 总更新次数 | 单步最大更新次数 |
|---|---:|---:|
| FP64 | 2087 | 6 |
| FP32 | 2087 | 6 |
| FP16 | 2088 | 6 |
| tensor scale | 2089 | 6 |
| element-Prony scale | 2089 | 6 |

可以支持的有限结论：在这一算例、网格和时间区间内，低精度历史存储引入的位移差异较小，Newton 更新次数变化很小。

不能支持的结论：所有工况均稳定、所有网格均准确、scaled FP16 总是更优、或者整体求解器获得某一比例加速。日志中的 Hessian assembly time 不是完整墙钟运行时间。

## 9. 尚未解决的 r=50 scaled FP16 NaN

这是当前进度中必须保留的问题，不能在写作时忽略或当作已经修复。

现有超算截图显示：

- r=50、FP16 + element-Prony scale：Step 0 收敛；首次历史更新后，Step 1 的第一次内力计算出现 NaN，随后求解量也变为 NaN。
- 同分辨率 FP64：已观察到通过该时间步并继续推进，但截图不证明完整 T=3 已完成。
- 同分辨率普通 FP16：也通过了对应时间步，但同样不能据此宣称完整运行成功。
- tensor scale 在该配置下的对照结果尚待确认。

目前怀疑 scaling 相关路径，但根因尚未确认。可能涉及 scale 的构造或恢复、索引、异常数值、并行或平台相关行为，不能把任何一种猜测写成确定结论。

下一步应在首次历史更新前后定位第一个非有限值，检查更新前历史、更新值、scale、归一化值、FP16 写入与恢复后的值，以及相关索引。修复后应复核受影响结果。

## 10. 后续实验：最小闭环

### 必须完成

1. 确认用于论文的所有 case 完整运行，并保存参数、版本、日志和输出。
2. 完成五种策略在选定基准网格上的完整耦合比较。
3. 补充至少一个更细网格；若时间允许，再增加第三个分辨率。
4. 每个分辨率分别以自身 FP64 为参考，统计最终和全时间区间采样峰值误差。
5. 核算持久历史及 scale 内存，记录迭代与失败情况。
6. 对 r=50 的 NaN 完成定位与复测；若截止前不能完成，不得对该配置作成功或稳定性声明，并与老板确认缩减范围。

### 时间允许再加入

- per_elem FP64 与 per-QP FP64 的独立比较，再考虑叠加低精度。
- 完整运行时间与峰值内存测量；未测量则不作性能或 RSS 声明。

网格实验的目标是观察“相对同网格 FP64 的存储误差”是否随分辨率变化，不自动等同于严格的有限元网格收敛证明。

## 11. 论文结构

### 1. Introduction

应用动机、历史存储成本、相关工作、本文研究问题与有限贡献。

### 2. Finite-strain viscoelastic model and discretization

Mooney–Rivlin 弹性部分、Prony 分支与历史更新、HEX8 离散、时间积分及非线性求解。明确历史参与当前平衡，但收敛后才提交新历史。

### 3. Reduced-precision history storage

五种策略、精度与 scale 布局、内存公式和比例；若包含 per_elem，在最后单独说明其空间近似性质。

### 4. Numerical experiments and discussion

悬臂梁、材料、载荷、边界、时间和求解参数；误差测量定义；基准网格结果；网格敏感性；收敛表现及已知限制；可选 per_elem 结果。

### 5. Conclusions

总结历史存储节省与观察到的位移误差，限定适用范围。严格上界、长期行为、更多工况和系统性 robustness 分析列为 future work。

## 12. 建议保留的图表

| 图表 | 提供的证据 |
|---|---|
| 悬臂梁示意图 | 几何、载荷、约束与自由端测量位置 |
| 存储策略及内存表 | 节省来自何处，scale 成本是多少 |
| 自由端响应与全场误差图 | 完整耦合解与 FP64 参考的差异 |
| 不同分辨率误差表或图 | 存储误差对网格的敏感性 |
| 收敛统计表，可与误差表合并 | 对非线性求解的影响与失败情况 |
| 可选 per_elem 比较 | 空间历史压缩的额外代价 |

## 13. 给论文 Codex 的约束

- 暂时不把 replay 纳入摘要、贡献、方法或实验计划。
- 不扩展成严格误差上界或长期稳定性论文。
- 不编造未完成的网格、运行数据、文献结论或 NaN 根因。
- 不混淆 element-Prony scale 与 per_elem history。
- 不把历史 buffer 节省等同于整个进程内存节省。
- 不把节点欧氏误差称作体积加权场误差。
- 不把自由端节点平均位移称作中轴线或中心点位移。
- 不把 FP64 数值参考称作解析真解。
- 不将暂时正常的前几个时间步写成完整仿真成功。
- 表中已完成数据来自本地 r=15，超算结果必须独立检查后再补充。
- 写出最终公式、参数表和数字前，用当前代码、CSV 与日志复核；本文档是交接材料，不替代最终核验。

核心源码定位：

- `frontend/tests/sfem_MooneyRivlinGravityTest.cpp`
- `frontend/ops/sfem_MooneyRivlinVisco.cpp`
- `operators/mooney_rivlin_visco.cpp`
- `operators/hex8/hex8_mooney_rivlin_visco_flexible.cpp`
- `operators/hex8/hex8_mooney_rivlin_visco_unique_Hi_local.hpp`
