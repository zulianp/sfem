# Example poisson
# ssolve config.yaml

# date: ${SIMULATION_DATE}

mesh: 
  type: file
  path: ${MESH_PATH}

dirichlet:
  type: "value"
  idx:  ${DIRICHLET_PATH}
  value: 0.

neumann:
  type: "value"
  idx: ${NEUMANN_PATH}
  value: 1.

out: ${OUTPUT_PATH}
